{
  "name": "workers",
  "version": "1.0.0",
  "repository": "https://github.com/sterlingwes/fediship-port.git",
  "author": "Wes Johnson <wes@swj.io>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "cron:publish": "wrangler publish --config cron-instance-info.toml",
    "cron:tail": "wrangler tail --config cron-instance-info.toml",
    "cron:dev": "wrangler dev --config cron-instance-info.toml",
    "api:publish": "wrangler publish --config api-worker.toml",
    "api:tail": "wrangler tail --config api-worker.toml",
    "api:dev": "wrangler dev --config api-worker.toml",
    "types": "tsc --noEmit",
    "test": "echo no tests for workers",
    "build": "echo no build for workers"
  },
  "dependencies": {
    "nanoid": "^3.3.4"
  }
}
